<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nutz.dimdamlalwaniromero_midtermexam">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material.Light.NoActionBar.FullScreen">
        <activity android:name=".StartupScreen.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StartupScreen.Signup" />
        <activity android:name=".StartupScreen.Login" />
        <activity android:name=".Shop.Shop" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".StartupScreen.WelcomeScreen" />
        <activity android:name=".Admin.Admin" />
        <activity
            android:name=".Cart.Cart"
            android:label="@string/title_activity_cart"></activity>
    </application>

</manifest>